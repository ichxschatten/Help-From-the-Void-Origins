apply plugin: 'com.android.application'

android {
    compileSdkVersion 33
    buildToolsVersion "33.0.0"

    defaultConfig {
        applicationId "hftvis.ichxtenebra.help_from_the_void_origins"
        minSdkVersion 21
        targetSdkVersion 33
        versionCode 1
        versionName "0.0.0.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}
